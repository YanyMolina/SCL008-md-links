{
  "name": "md-links",
  "version": "0.1.0",
  "dependencies": {
    "jest": "^24.7.1",
    "markdown-link-extractor": "^1.2.0",
    "node-fetch": "^2.3.0"
  },
  "scripts": {
    "test": "jest --coverage"
  }
}
